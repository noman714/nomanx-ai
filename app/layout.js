export const metadata = {
  title: "NomanX AI",
  description: "Your Creative AI Partner",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
